import { Card, CardTitle, SaveButton } from './styles'

export function AccountInfo() {
  return (
    <Card>
      <CardTitle>Account info</CardTitle>
      <SaveButton>Save</SaveButton>
    </Card>
  )
}
